# Copy this file to .env and fill in your API key.
# That's the only setup required to run this project.

OPENAI_API_KEY=sk-...

# Optional: override the default model (gpt-4o-mini is fast and cheap)
# OPENAI_MODEL=gpt-4o
